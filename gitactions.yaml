# Legado: workflows oficiais estao em .github/workflows/
#   deploy-kubernetes.yml  -> servidor K8s (SSH)
#   deploy-azure.yml       -> Terraform Azure
# Copie este arquivo para .github/workflows/ apenas se ainda nao tiver os novos.