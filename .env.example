# Copie para .env na raiz do projeto (opcional; docker-compose lê variáveis do shell)

# Assistente no painel (chat via API Java → DeepSeek)
DEEPSEEK_API_KEY=

# MongoDB (padrão já definido no compose)
# MONGODB_URI=mongodb://admin:admin123@mongodb:27017/fraud_detection?authSource=admin
